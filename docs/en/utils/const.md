# Constants

Defines common constants in the `G3-Web` project.

## Rest-Api Address

`restUrl`

- Gets the REST API URL of the current WordPress site.
- Returns an empty string when running in a non-browser environment.
